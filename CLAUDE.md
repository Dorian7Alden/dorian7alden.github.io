# CLAUDE.md

这是 Dorian（Dorian Alden Dai）的个人博客文档仓库。所有内容均为中文撰写。

## 仓库分支架构

- **`notes` 分支（当前）**：纯粹的 Markdown 博客文档。所有内容以 `.md` 文件组织，按主题分类到不同目录。
- **`main` 分支**：博客网站的源代码，负责将 `notes` 分支的 `.md` 文档转换为 HTML 页面发布到博客网站。

**当前工作在 `notes` 分支**，只涉及 Markdown 文档的编写、整理和分类。

## 目录结构与分类体系

### 计算机技术类

| 目录 | 用途 |
|------|------|
| `计算机/` | 计算机技术学习笔记（AI学习、Java学习、小林coding面试题、工具使用、提示词、计算机四大件） |
| `学习/` | 与 `计算机/` 内容重叠，额外包含 JsRE、Linux、汇编语言等。**两目录存在大量重复文件，后续需要去重合并** |
| `JavaWeb学习/` | Java Web 学习路径（Tomcat、Servlet、Maven、MyBatis、SpringSecurity、Spring） |
| `卡片/` | 原子化知识卡片，编号前缀 `01-` 到 `31-`，每张卡片记录一个独立的技术问题或踩坑经验 |
| `AI资料/` | AI 相关的参考资料（前端设计、提示词优化器） |
| `AI踩坑/` | AI 使用中的踩坑记录 |
| `项目/` | 个人项目笔记（Harness、Skill、TODO可视化时间表、英语学习app） |

### 竞赛与课程类

| 目录 | 用途 |
|------|------|
| `Competitions/` | 竞赛笔记（Algorithm 算法竞赛、MCM 数学建模、UIEP） |
| `作业/` | 大学课程作业（C++实验、Web实验报告、软工导论、形式政策、职业生涯规划等） |

### 个人成长类

| 目录 | 用途 |
|------|------|
| `感想/` | 个人感想与思考 |
| `成长记录/` | 个人成长记录 |
| `实习准备/` | 实习准备材料（LeetCode Hot 100、开发规范、面试题） |
| `实习计划/` | 实习计划与问题整理 |
| `招聘记录/` | 招聘会记录与背景调研 |
| `我的项目/` | 个人项目详细文档 |

### 非技术类

| 目录 | 用途 |
|------|------|
| `非计算机/` | 非技术内容 — Daily English Dictation 每日英语听写练习、日常学习卡片 |

### 待整理

- `待整理的内容.md` — 待整理的内容提示
- `ai做数学题.md` — AI 做数学题的提示词
- `README.md` — 仓库简介

## 内容写作风格

- **语言**：全部中文撰写，代码片段和技术术语保留英文。
- **卡片格式**（`卡片/` 目录）：编号前缀 + 简短标题，如 `01-AI-api解析失败.md`。每张卡片记录一个具体技术问题和解决方案，篇幅短小精悍。部分新卡片使用 YAML frontmatter（`create-time`、`update-time`）。
- **Daily English Dictation**（`非计算机/Daily English Dictation/`）：按 10 篇一组分目录（`001-010`、`011-020` 等），每篇命名 `Dictation-XXX.md` 或 `XXX-{level}.md`。使用固定模板（`template.md`），包含 Listening Practice 和 Accumulation 两大板块。
- **日常学习卡片**（`非计算机/日常学习卡片/`）：按月份和星期组织（`26-03/week1/day1.md`）。
- **笔记文档**：自由格式 Markdown，无强制模板。通常包含问题背景、解决方案、代码片段、反思总结。

## Claude Code 配置

仓库在 `.claude/skills/` 下有两个自定义 Skill：

- **DailyEnglishDictation英语积累**：从英语听写句子中自动提取词汇、短语、语法结构、发音现象，生成结构化积累卡片。
- **格式规范定义**：定义数据结构格式规范的符号系统（可选字段 `[ ]`、重复 `*`/`+`、枚举 `{}`、条件依赖等）。

## 当前已知问题与待办

1. **`计算机/` 与 `学习/` 目录内容重复**：两个目录下有大量相同文件（AI学习、Java学习、小林coding、工具使用、提示词），需要决定合并策略并去重。
2. **`卡片/` 编号不统一**：前 31 张卡片使用数字编号前缀，但夹杂了 `2026-04-27.md` 这种日期命名。需要统一命名规范。
3. **`非计算机/Daily English Dictation/` 文件命名不统一**：早期用 `Dictation-XXX.md`，后期用 `XXX-{level}.md`（如 `061-beginner.md`）。还有 `1-blue-beginner.md` 等颜色标签命名。需要统一。
4. **待整理内容**：`待整理的内容.md` 中标记的内容尚未归类。
5. **根目录散落文件**：`ai做数学题.md` 等文件需要归入合适目录。

## 工作约定

- 在 `notes` 分支上工作，不涉及网站构建相关操作。
- 新增文档时放入对应的分类目录，避免在根目录创建新 `.md` 文件。
- `卡片/` 中新卡片使用递增数字编号作为前缀。
- Daily English Dictation 新练习放入对应的 10 篇一组子目录，统一使用 `XXX-{level}.md` 命名。
- 编辑已有文档时保留其原有格式风格。
- `.gitignore` 中排除了 `TODO.md` 和 `我的灵感.md`。
