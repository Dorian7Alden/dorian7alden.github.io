# RMDB

## 项目背景

RMDB 是一个关系型数据库管理系统代码框架，用于**全国大学生计算机系统能力大赛数据库管理系统赛道**。参赛队伍在此基础上设计和实现完整的数据库内核，要求具备运行 TPC-C 基准测试常用负载的能力。

项目采用[木兰宽松许可证 v2](https://license.coscl.org.cn/MulanPSL2)。

## 总目标

**在 `main-zero` 分支上逐题集成，最终通过全部 10 道在线评测题目，每题满分。**

在线评测通过提交指定分支完成。每道题有对应的满分参考分支（见下方表格），需要对比差异、整合代码，每次改动后验证不引入回归。

## 项目现状

当前在 `main-zero` 分支从零开始。各题满分实现散落在独立分支上，需要在 `main-zero` 上逐一整合。

核心挑战：修改后面题目的代码时，可能会破坏前面已通过的题目。每次改动都需验证，确保**不会引入回归问题**。

### 🚫 铁律（绝对禁止）

在 `main-zero` 分支未确认 10 题全满分之前，**严禁**以下操作：

- ❌ **删除或修改**任何现有的满分分支。
- ❌ **在未经允许的情况下合并分支**。

> 以上规则没有例外。

### 满分分支对应关系

| 题目编号 | 满分分支              | 集成状态 |
|:---------|:---------------------|:--------:|
| 第 1 题  | main                 | ✅ 已集成 |
| 第 2 题  | main                 | ✅ 已集成 |
| 第 3 题  | task03UniqueIndex    | ✅ 已集成 |
| 第 4 题  | task04QueryOptimization-β | ⬜ 待集成 |
| 第 5 题  | task06Union          | ⬜ 待集成 |
| 第 6 题  | task06Union          | ⬜ 待集成 |
| 第 7 题  | task07Join-β         | ⬜ 待集成 |
| 第 8 题  | task08Transaction    | ⬜ 待集成 |
| 第 9 题  | task09Isolation-Ω    | ⬜ 待集成 |
| 第 10 题 | taskPerformance      | ⬜ 待集成 |

### 当前 `main-zero` 分支测评结果

| 题目编号 | 当前得分 | 满分分值 |
|:---------|:--------:|:--------:|
| 第 1 题  | 5        | 5        |
| 第 2 题  | 5        | 5        |
| 第 3 题  | 10       | 10       |
| 第 4 题  | 0        | 10       |
| 第 5 题  | 0        | 10       |
| 第 6 题  | 0        | 10       |
| 第 7 题  | 0        | 15       |
| 第 8 题  | 0        | 5        |
| 第 9 题  | 0        | 20       |
| 第 10 题 | 0        | 10       |
| **合计** | **20**   | **100**  |

## 开发场景

### 在线评测（10 道题）

题目的详细描述和子任务在 `docs/tasks/` 下，按编号一一对应。

### 性能评测

10 道题全部满分通过后，再进行性能分析和调优。

## 目录结构

```
db2026-x/
├── src/            # 主源码，入口 rmdb.cpp:main()
├── docs/
│   ├── tasks/      # 10 道题的题目描述
├── deps/           # 依赖（googletest）
├── build/          # CMake 构建输出
├── test/           # 测试文件目录
└── CMakeLists.txt  # 项目根构建配置
```

src/ 子目录之间的依赖自底向上：common → storage/replacer → record/index → system → transaction/recovery → execution → analyze/optimizer → parser。test 为测试基础设施。

## 常用命令

```bash
# 构建（在项目根目录下）
cd build && cmake .. && make -j$(nproc)

# 构建并运行单元测试
cd build && cmake .. && make -j$(nproc) && ctest

# 构建单个目标
cd build && make rmdb        # 主程序
cd build && make unit_test   # 单元测试
```

## 要求

- CLAUDE.md 中的文件索引不要放所有文件，只需要给关键文件/文件夹进行说明就行了，每一层应该通过对应的 README.md 进行说明，渐进式披露，而不是把所有介绍都聚合到一起，低耦合。
- 所有回答内容用中文
- 目录中存在 README.md 文件时，一定要先加载 README.md 了解情况
- 修改代码前先确认当前分支和题目目标
- 每次集成新题目后，必须验证前序题目不出现回归
- 各题满分参考分支仅供对比参考（铁律已覆盖，不再重复）
- **禁止在未经允许的情况下合并分支**
- **禁止在未经允许的情况下合并分支**
- **禁止在未经允许的情况下合并分支**
- 各题满分参考分支不可删除或修改，仅供对比参考
- 各题满分参考分支不可删除或修改，仅供对比参考
- 各题满分参考分支不可删除或修改，仅供对比参考
- 严禁将 ignore 的文件或文件夹添加到git的修改记录，不允许强制追踪

