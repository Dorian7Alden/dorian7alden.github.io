# AI使用经验汇总 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `archives/AI/AI感想.md` 的零散随笔整理为独立终稿 `archives/AI/02-AI使用经验汇总.md`（结构化经验库），原文保留不动。

**Architecture:** 按设计文档方案 A：类型分章（原则 / 机制经验 / 构想 / 待办）+ 章内主题分节；每条统一「类型标签 + 结论先行 + 可选场景/机制/建议」；附录含整理说明与原文对照表。内容只依据设计文档条目清单与第 7 节澄清表扩写，不编造。

**Tech Stack:** Markdown；仓库文档规范 `.docs/03-文档撰写规范.md`；设计依据 `docs/superpowers/specs/2026-07-18-ai-usage-experience-design.md`。

## Global Constraints

- 只新建 `archives/AI/02-AI使用经验汇总.md`；**禁止修改** `archives/AI/AI感想.md` 与 `archives/AI/01-AI 发展对大学生的影响.md`。
- 默认**不修改** `archives/AI/README.md`（除非用户另令）。
- 文风：工整偏书面；结论先行；无脏话/过激口语。
- 构想章每条必须有 **状态：未验证**。
- 配置与 Memory：**一律项目级**；禁止依赖全局统一规范（见条目 3.4.5）。
- 2.11、2.12 写在原则章，不写在构想章。
- 标题：文档题 `##`；章 `###`；节与条 `####`；标题上下文空一行。
- 原则/经验正文中不出现「TBD/TODO」字样；待办只在第 5 章。
- 提交信息遵循 `docs(AI): 中文简述`；未获用户同意不 commit 其它无关文件。

---

## File Map

| 文件 | 职责 |
|------|------|
| `docs/superpowers/specs/2026-07-18-ai-usage-experience-design.md` | 只读：条目 ID、一句话结论、澄清语义、质量清单 |
| `archives/AI/AI感想.md` | 只读：扩写依据与附录行号对照 |
| `archives/AI/02-AI使用经验汇总.md` | 唯一写入产物：终稿全文 |
| `.docs/03-文档撰写规范.md` | 只读：标题层级与空行 |

---

### Task 1: 搭建终稿骨架与第 1 章文档说明

**Files:**
- Create: `archives/AI/02-AI使用经验汇总.md`
- Read: `docs/superpowers/specs/2026-07-18-ai-usage-experience-design.md`（§2、§4、§5、§8.1）
- Read: `.docs/03-文档撰写规范.md`（基本格式）

- [ ] **Step 1: 确认产物路径不存在且原文仍在**

Run:

```bash
test ! -f archives/AI/02-AI使用经验汇总.md && test -f archives/AI/AI感想.md && echo OK
```

Expected: `OK`

- [ ] **Step 2: 创建终稿文件，写入 frontmatter、文档标题、完整目录骨架、第 1 章**

写入 `archives/AI/02-AI使用经验汇总.md`，内容如下（可原样使用；后续 Task 只在对应章标题下追加条目，不改骨架标题用词）：

```markdown
---
create-time: 2026-07-18
update-time: 2026-07-18
---



## AI使用经验汇总



### 1. 文档说明



本文档从日常使用 AI 工具时的零散随笔整理而来，目标是形成可长期增补的**结构化经验库**。



- **来源**：`archives/AI/AI感想.md`（原文保留，本文件为整理稿）
- **整理日期**：2026-07-18
- **类型标签**：
  - `原则`：可反复套用的判断标准
  - `经验`：机制观察或踩坑，附可执行建议
  - `构想`：尚未验证的产品或工作流点子
  - `待办`：计划学习、工程化或收集的事项
- **阅读建议**：先读第 2 章建立总原则，再按问题域查阅第 3 章；第 4 章为未验证构想，实施前需单独验证；第 5 章为清单。
- **续写约定**：新想法先判定类型，再写入对应章节；构想必须标明「未验证」；项目相关规范与 Memory 一律按**项目级**维护，不依赖全局统一规范。
- **关联**：与 `01-AI 发展对大学生的影响.md` 相互独立，互不替代。



### 2. 使用原则与方法论



### 3. 机制与工具经验



#### 3.1 上下文与记忆



#### 3.2 任务委派与 Agent 结构



#### 3.3 规则、Skill 与提示词



#### 3.4 Claude Code 与配置管理



#### 3.5 编码、文档与维护策略



#### 3.6 用 AI 学习



### 4. 产品与工作流构想



#### 4.1 交互与角色



#### 4.2 教程与知识生产



#### 4.3 记忆、话题与工作流可视化



#### 4.4 工具链与辅助能力



### 5. 待办与学习清单



#### 5.1 学习类



#### 5.2 工程 / 工具类



#### 5.3 资料收集类



#### 5.4 方法类



### 6. 附录



#### 6.1 整理说明



#### 6.2 条目与原文对照

```

- [ ] **Step 3: 校验骨架**

Run:

```bash
rg -n "^## |^### |^#### |create-time|类型标签|项目级" "archives/AI/02-AI使用经验汇总.md" | head -80
```

Expected: 存在 `## AI使用经验汇总`、`### 1`…`### 6`、各 `#### 3.x` / `4.x` / `5.x` / `6.x` 标题；第 1 章含类型标签与项目级说明。

- [ ] **Step 4: 确认未改动原文**

Run:

```bash
git status --short archives/AI/
```

Expected: 仅出现 `?? archives/AI/02-AI使用经验汇总.md`（或未跟踪新文件）；**不应**出现 `M archives/AI/AI感想.md` 或对 `01-…` 的修改。

- [ ] **Step 5: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 搭建AI使用经验汇总文档骨架

EOF
)"
```

---

### Task 2: 撰写第 2 章（原则 2.1–2.12）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（`### 2. 使用原则与方法论` 下）
- Read: design spec §6.1、§7（反问句、看懂≠会用、质量优先、思维链）

**条目模板（本章每条使用）：**

```markdown
#### <条目标题>

**类型**：原则

<一句话结论。>

- **建议做法**：…
- **备注**：（可选）
```

- [ ] **Step 1: 在 `### 2. 使用原则与方法论` 下按顺序写入 2.1–2.12**

必须覆盖且标题与设计文档一致：

| ID | 标题 | 写作要点（不得偏离） |
|----|------|----------------------|
| 2.1 | AI 不能替代系统设计 | 一句话开发的错觉；缺设计→逻辑脆弱、事后修 bug、维护成本高 |
| 2.2 | Vibe Coding 宜先探坑再正式设计 | 先试做暴露麻烦，再正式设计；不是跳过设计 |
| 2.3 | 人必须监管与决策 | 放弃「AI 不停写完产品」；人验收决策 |
| 2.4 | 多次小任务优于一次大包 | 长任务易偷懒/偏航；多轮可验收小步 |
| 2.5 | 质量优先于「能跑多久」 | 符合预期、可维护、成本可控；交叉引用即可，不在第 4 章重复长写 |
| 2.6 | 资料与知识库重于反复调 Prompt | 提示词难复用；规范/范例/知识库可复用 |
| 2.7 | 看懂 ≠ 会用 ≠ 能讲懂 | AI 讲解易有学会错觉；能讲清、跑通流程才算内化 |
| 2.8 | 用陈述补上下文，不用反问纠错 | 禁用「难道不该是…」式纠错；改为明确陈述与补背景 |
| 2.9 | 正向说明优先于堆反向约束 | 写期望行为；少堆「不要…」 |
| 2.10 | 先跑通再优化 | 先可运行，再结构与质量 |
| 2.11 | 执行要有思维链与原因 | 「该这么做」：关键操作说明原因 |
| 2.12 | 先收集已知条件再切入 | 条件→切入点→分析→澄清→方案→执行 |

每条 80–200 汉字为宜；书面语；无脏话。

- [ ] **Step 2: 校验第 2 章完整性**

Run:

```bash
rg -n "^\#### " "archives/AI/02-AI使用经验汇总.md" | rg "2\.|AI 不能|Vibe|监管|小任务|质量优先|知识库|看懂|反问|正向|先跑通|思维链|已知条件"
test "$(rg -c '^\*\*类型\*\*：原则' 'archives/AI/02-AI使用经验汇总.md')" -ge 12 && echo "principles_ok"
```

Expected: 12 条原则标题均在；`principles_ok`。

- [ ] **Step 3: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 撰写使用原则与方法论章节

EOF
)"
```

---

### Task 3: 撰写第 3 章 3.1–3.3（上下文、委派、规则/Skill）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（`#### 3.1`–`#### 3.3` 下）
- Read: design spec §6.2（3.1–3.3）、§7（上下文拼接、规则衰减、文科脚本、f-string bug、边界用例）
- Read: `archives/AI/AI感想.md` 相关段落（只读）

**条目模板（经验）：**

```markdown
#### <条目标题>

**类型**：经验

<一句话结论。>

- **现象 / 场景**：…
- **原因 / 机制**：…（可省略）
- **建议做法**：…
- **备注**：…（可省略；产品行为变化时写「曾观察到」）
```

- [ ] **Step 1: 写入 3.1 全部 5 条**

| ID | 标题 | 要点 |
|----|------|------|
| 3.1.1 | 上下文永远不完整 | 内心默认理解、Git 等动态状态未写入则会被模型误假设 |
| 3.1.2 | 网页对话 vs Agent 工具的上下文形态 | 网页≈多轮 Q/A 拼接；Agent 含思考/工具/命令结果；拼装细节待深入（可链 5.1.2） |
| 3.1.3 | 摘要替换上下文，原始记录另存 | 上下文用摘要；原始记录保留 |
| 3.1.4 | 生成内容有「缓存变量」效应 | 手改文件后须让 AI 重读，否则按旧生成内容改 |
| 3.1.5 | 主会话宜委派，细节交给 Sub-agent | 主会话分发与验收 |

- [ ] **Step 2: 写入 3.2 全部 4 条**

| ID | 标题 | 要点 |
|----|------|------|
| 3.2.1 | 分层委派，上层只看摘要 | Boss/Worker；**不要**写成固定「必须三层」；备注可写三层为可选延伸 |
| 3.2.2 | 不同任务用不同策略 | 策略随任务类型变化 |
| 3.2.3 | 执行前先对齐任务理解 | 复述目标与步骤再开工 |
| 3.2.4 | Agent 默认被动，需主动补上下文 | 未读全约束易偏差 |

- [ ] **Step 3: 写入 3.3 全部 8 条**

| ID | 标题 | 要点 |
|----|------|------|
| 3.3.1 | 规则会衰减，且会与 Skill 冲突 | 长上下文冲淡规则；与会自动 commit 的 Skill（如 brainstorming）冲突；写「曾观察到」 |
| 3.3.2 | 规则有例外，要写清优先级 | 例外原因 + 决策记录 |
| 3.3.3 | Skill 宜通用，勿绑定单次会话状态 | 换会话失效 |
| 3.3.4 | Skill 要区分常量与变量 | 并做效果测试 |
| 3.3.5 | 用边界用例检验 Prompt/Skill | 「1 杯 / 99 杯 / 蛋炒饭」；检验对象是**提示词或 Skill**，不是 token |
| 3.3.6 | 小概率场景用事后检查，不塞常驻规则 | Python：`f"..."` 换行与 `"-" * 30` 错误结合导致整段被乘法刷屏；示例代码可保留最小片段；非常驻 prompt |
| 3.3.7 | 格式转换：外层勿套代码块；可脚本+AI 润色 | 防 MD 外层套 fence；HTML→md 脚本先行；纯 AI 转 PDF 质量差 |
| 3.3.8 | 文科资料整理慎用 AI 编造与脚本替换 | 模型易脱离原文；多轮正则替换误匹配、难回滚 |

3.3.6 建议最小代码示例（说明错误形态即可）：

```python
# 错误形态示意：乘法作用域与 f-string 拼接不当，可能导致大段内容被重复
f"content\n\n"
f"-" * 30 + f"\n\ncontent"
```

- [ ] **Step 4: 校验 3.1–3.3 条数与关键措辞**

Run:

```bash
rg -n "^\#### " "archives/AI/02-AI使用经验汇总.md"
rg -n "项目级|未验证|三层|token 是否|傻逼|大便" "archives/AI/02-AI使用经验汇总.md" || true
```

Expected:
- 3.1 下 5 个条目标题、3.2 下 4 个、3.3 下 8 个（另有节标题本身）。
- **不得**出现脏话原文；**不得**把边界用例写成「判断 token」；分层委派**不得**写成强制三层架构。

- [ ] **Step 5: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 撰写上下文委派与规则经验

EOF
)"
```

---

### Task 4: 撰写第 3 章 3.4–3.6（配置、编码维护、学习）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（`#### 3.4`–`#### 3.6`）
- Read: design spec §6.2（3.4–3.6）、§7（Memory 项目级）

- [ ] **Step 1: 写入 3.4 全部 5 条**

| ID | 标题 | 要点 |
|----|------|------|
| 3.4.1 | ignore 的文件也可能被插件强行纳入 Git | 插件/流程写入工作记录；语气克制，可写「曾观察到」 |
| 3.4.2 | 仓库 ignore 配置文件会导致多分支配置混乱 | ignore Claude 配置 → 分支间不一致 |
| 3.4.3 | CLAUDE.md 放稳定说明，不放具体任务清单 | 任务另文件；目录与固定约定 |
| 3.4.4 | 配置集中管理，且按项目区分 | 集中目录 + **项目级** |
| 3.4.5 | Memory 与规范采用项目级 | **禁止依赖全局统一规范**；不同项目不同要求——用户明确规范，必须写死 |

- [ ] **Step 2: 写入 3.5 全部 9 条**

按 design §6.2 表：事后修 bug、先定位结构、数据获取逻辑一致、分散维护、挂载式维护、文档二次确认、长文本先备份、不主动关联 Git/他人进度、前端 Demo 组件流。

- [ ] **Step 3: 写入 3.6 全部 4 条**

按 design §6.2 表：AI 提问链、讲解要能运用（链 2.7）、先验规范再开发、开源范式提炼。

- [ ] **Step 4: 校验 3.4.5 与第 3 章经验条类型**

Run:

```bash
rg -n -A6 "Memory 与规范采用项目级|禁止依赖全局" "archives/AI/02-AI使用经验汇总.md"
rg -c '^\*\*类型\*\*：经验' "archives/AI/02-AI使用经验汇总.md"
```

Expected: 3.4.5 含「项目级」与「禁止」全局规范语义；经验类型计数 ≥ 5+4+8+5+9+4 = **35**。

- [ ] **Step 5: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 撰写配置维护与学习经验

EOF
)"
```

---

### Task 5: 撰写第 4 章（构想，一律未验证）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（`### 4` 下各节）
- Read: design spec §6.3、§7（傻子提问者、反思 Agent、Shell+Monitor 等）

**条目模板（构想）：**

```markdown
#### <条目标题>

**类型**：构想  
**状态**：未验证

<一句话定义。>

- **设想**：…
- **备注**：与 x.x 的关系 / 待验证点（可选）
```

- [ ] **Step 1: 写入 4.1（5 条）**

反思 Agent；Asker+表单；项目经理 Skill；多角色协作；Agent 聊天室。  
4.1.1 须体现：被动一问一答不足，需主动补上下文、打磨质量（类「优秀同学」主观能动性），但保持书面语。

- [ ] **Step 2: 写入 4.2（6 条）**

傻子提问者；写书式讲解/写稿人（连贯、新概念必释、已讲引用）；自动补上下文 Skill；示例 Skill；标签 Skill；母子技能。  
4.2.1 与 4.2.2 互相备注交叉引用。

- [ ] **Step 3: 写入 4.3（7 条）**

记忆可视化；话题头；猜大话题；工作流看板；伪代码→工作树；纠正持久化；从 Skill 抽习惯。

- [ ] **Step 4: 写入 4.4（8 条）**

Shell+Monitor（执行 + 盯任务 callback，免阻塞）；提示词安全；语音+Optimizer；脱敏；打磨器；探索模式；快速选型包；Skill 多轮确认。

**禁止**在本章展开 2.11/2.12 或长写 2.5；若提及只用「参见 2.x」一句。

- [ ] **Step 5: 校验构想章**

Run:

```bash
rg -c '^\*\*状态\*\*：未验证' "archives/AI/02-AI使用经验汇总.md"
rg -c '^\*\*类型\*\*：构想' "archives/AI/02-AI使用经验汇总.md"
rg -n "思维链|已知条件|能跑多久" "archives/AI/02-AI使用经验汇总.md" || true
```

Expected: `未验证` 与 `构想` 计数均为 **26**（5+6+7+8）；「思维链/已知条件」若出现，应只在第 2 章或「参见」短引，不在第 4 章作独立长条目。

- [ ] **Step 6: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 撰写产品与工作流构想章节

EOF
)"
```

---

### Task 6: 撰写第 5 章（待办清单）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（`### 5` 下）
- Read: design spec §6.4

**条目模板（待办）：**

```markdown
#### <条目标题>

**类型**：待办  
**状态**：未开始

<一句话说明要做什么。>

- **备注**：来源或验收大致标准（可选）
```

- [ ] **Step 1: 写入 5.1 学习类 3 条**

| ID | 标题 | 要点 |
|----|------|------|
| 5.1.1 | 学习「上下文工程的搭建」 | 仅有必要性意识，需系统学习（非已有结论） |
| 5.1.2 | 弄清 Agent 工具链上下文如何拼装 | 思考/工具/命令结果如何进入请求；呼应 3.1.2 |
| 5.1.3 | 知识存储形态 | 原文 vs 向量及利弊 |

- [ ] **Step 2: 写入 5.2 工程类 6 条**

导出对话；字数 Skill；自动同步配置（**项目级**）；多项目 Skill 维护；前端工作流文档化；提问与手册分档。

- [ ] **Step 3: 写入 5.3 与 5.4**

- 5.3.1 一人公司/OPC 经验搜集总结  
- 5.4.1 问题→设计思想归类  
- 5.4.2 记忆原始+提炼双轨  

- [ ] **Step 4: 校验待办**

Run:

```bash
rg -c '^\*\*类型\*\*：待办' "archives/AI/02-AI使用经验汇总.md"
test "$(rg -c '^\*\*类型\*\*：待办' 'archives/AI/02-AI使用经验汇总.md')" -eq 12 && echo todos_ok
```

Expected: `todos_ok`（3+6+1+2=12）

- [ ] **Step 5: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 撰写待办与学习清单

EOF
)"
```

---

### Task 7: 撰写附录（整理说明 + 原文对照表）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（`### 6`）
- Read: `archives/AI/AI感想.md`（用 `rg -n` 定位行号，只读）
- Read: design spec §6.5、§7

- [ ] **Step 1: 写 `#### 6.1 整理说明`**

必须包含：

- 来源：`archives/AI/AI感想.md`  
- 整理日期：2026-07-18  
- 类型标签含义（四类）  
- 原文保留、本文件为整理稿  
- 对照表行号为整理时约略位置  
- 续写约定与项目级规范提醒  

- [ ] **Step 2: 用 rg 为主要条目定位原文行号**

在仓库根目录执行（示例；可按需增补关键词）：

```bash
rg -n "系统设计|vibe coding|上下文永远|聊天记录直接拼接|shell and monitor|傻子提问者|agent的竞争力|反问|CLAUDE|自动 commit|文科|f\"|缓存|一人公司|上下文工程|质量" "archives/AI/AI感想.md"
```

- [ ] **Step 3: 写 `#### 6.2 条目与原文对照` 表格**

表头：

```markdown
| 终稿条目 | 原文关键词或摘录 | 原文约略位置 |
|----------|------------------|--------------|
```

要求：

1. **至少覆盖**全部原则 2.1–2.12、全部经验 3.x、全部构想 4.x、全部待办 5.x 的主条目（一行一条；一条可对应多段时用分号合并位置）。  
2. 位置写作 `约 L8`、`约 L360` 或 `约 L86–L93` 形式。  
3. 表前一句说明：行号基于 2026-07-18 的 `AI感想.md`，后续原文若编辑可能漂移。  
4. 下列澄清条必须能在表中回溯到原文关键词：

| 终稿 | 原文线索（便于 rg） |
|------|---------------------|
| 2.5 | agent的竞争力 / 质量＞数量 |
| 2.7 / 2.8 | 雪冬或讲解 / 反问句 |
| 3.1.2 | 聊天记录直接拼接 |
| 3.3.1 | commit / 遵守指令 |
| 3.3.6 | AI bug / 图片行附近 |
| 3.3.8 | 文科 / 脚本替换 |
| 3.4.5 | （规范来自整理对话；原文若无对应，位置写「整理对话补充」） |
| 4.1.1 | 反思人格 / 不会主动思考 |
| 4.2.1 | 傻子提问者 |
| 4.4.1 | shell and monitor |
| 5.1.1 | 上下文工程的搭建 |
| 5.3.1 | 个人公司 / 失败经历 |

- [ ] **Step 4: 校验附录与未改原文**

Run:

```bash
rg -n "^\| 2\.1|^\| 3\.1\.1|^\| 4\.1\.1|^\| 5\.1\.1|整理日期|约 L" "archives/AI/02-AI使用经验汇总.md" | head -40
git status --short archives/AI/AI感想.md archives/AI/01-AI\ 发展对大学生的影响.md
```

Expected: 对照表有多行 `约 L…`；`AI感想.md` 与 `01-…` **无**修改。

- [ ] **Step 5: Commit**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 补充经验汇总附录与原文对照

EOF
)"
```

---

### Task 8: 终稿通读质检（对照 design §8.3）

**Files:**
- Modify: `archives/AI/02-AI使用经验汇总.md`（仅修质检发现问题）
- Read: design spec §8.3

- [ ] **Step 1: 自动检查清单**

Run:

```bash
f="archives/AI/02-AI使用经验汇总.md"

echo "=== counts ==="
echo -n "原则 "; rg -c '^\*\*类型\*\*：原则' "$f"
echo -n "经验 "; rg -c '^\*\*类型\*\*：经验' "$f"
echo -n "构想 "; rg -c '^\*\*类型\*\*：构想' "$f"
echo -n "待办 "; rg -c '^\*\*类型\*\*：待办' "$f"
echo -n "未验证 "; rg -c '^\*\*状态\*\*：未验证' "$f"

echo "=== forbidden ==="
rg -n "TBD|TODO|傻逼|大便| ent |雪冬" "$f" || echo "no_forbidden_hits"

echo "=== must_have ==="
rg -n "禁止依赖全局|项目级" "$f" | head
rg -n "^\### 2 |^\### 3 |^\### 4 |^\### 5 |^\### 6 " "$f"

echo "=== source intact ==="
git status --short archives/AI/AI感想.md "archives/AI/01-AI 发展对大学生的影响.md"
```

Expected counts：
- 原则 = 12  
- 经验 = 35  
- 构想 = 26  
- 待办 = 12  
- 未验证 = 26  

Expected：`no_forbidden_hits`（第 5 章若绝对需要「待办」二字作标题可以，但不要 `TODO:`/`TBD` 占位）；原则/经验正文无 `TODO`/`TBD`；含项目级禁止全局；原文未改。

- [ ] **Step 2: 人工通读抽样**

打开终稿，人工确认：

1. 每条结论在标题下首段，不先堆现象。  
2. 构想无「已验证有效」类表述。  
3. 2.11、2.12 仅在第 2 章完整展开。  
4. 标题前后空行符合规范（无标题粘连）。  
5. 交叉引用（如 3.6.2→2.7、4.2.1↔4.2.2）存在且目标条目存在。

- [ ] **Step 3: 若有问题则就地修正，再跑 Step 1**

- [ ] **Step 4: 最终 Commit（仅当有修正时）**

```bash
git add "archives/AI/02-AI使用经验汇总.md"
git commit -m "$(cat <<'EOF'
docs(AI): 质检并完善AI使用经验汇总

EOF
)"
```

若 Step 1–2 零修改，可跳过本 commit，在实现报告中注明「质检通过无改动」。

- [ ] **Step 5: 向用户交付**

报告：

1. 终稿路径 `archives/AI/02-AI使用经验汇总.md`  
2. 条目统计（四类数量）  
3. 原文未改动确认  
4. 可选 follow-up：是否更新 `archives/AI/README.md` 索引（**等用户同意再做**）

---

## Self-Review（计划作者已执行）

| Spec 要求 | 对应 Task |
|-----------|-----------|
| 新建 `02-AI使用经验汇总.md` | Task 1 |
| 原则 2.1–2.12（含 2.11/2.12 下沉） | Task 2 |
| 经验 3.1–3.6 全表 | Task 3–4 |
| 构想 4.1–4.4 全表且未验证 | Task 5 |
| 待办 5.1–5.4 | Task 6 |
| 附录说明 + 原文对照 | Task 7 |
| 项目级 Memory/规范 | Task 4（3.4.5）+ Task 8 |
| 不改原文 / 不改 01 | Task 1/7/8 校验 |
| 书面语、质量清单 | Task 8 |
| 澄清表语义 | 各 Task 写作要点表 |

**Placeholder scan:** 计划中无「TBD/以后再写」；代码示例仅 3.3.6 错误形态示意。  
**一致性:** 条目数量 12+35+26+12；文件路径唯一产物；commit 均 `docs(AI):`。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-18-ai-usage-experience.md`.

**Two execution options:**

1. **Subagent-Driven（推荐）** — 每个 Task 新开子代理，Task 间你审查，迭代快  
2. **Inline Execution** — 本会话按 executing-plans 连续执行，设检查点  

**Which approach?**
