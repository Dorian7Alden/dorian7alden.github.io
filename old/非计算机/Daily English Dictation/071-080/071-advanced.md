---
create-time: 2026-06-11
update-time: 2026-06-11
---



## Daily English Dictation 71 - advanced



### 🎧 Listening Practice



**My listening**: 

```
You can asentually alter the updated new policy by manulate the leadings by your google web history. Or by simply not sign into sites like gmail when searching on google.
```

**Answer**: 

```
You can essentially opt out of  the new policy by manually deleting your Google web history or by simply not signing in to sites like Gmail when searching on Google.
```



### 📖 Accumulation



---

`essentially`    /ɪˈsenʃəli/

(adv.) 本质上，根本上:

- The two designs are <u>essentially</u> the same, just with different colors.

**Collocations**: `essentially the same`    `essentially correct`    `essentially important`    `essentially means`    `essentially unchanged`

**Related**: `basically`    `fundamentally`    `in essence`    `at its core`    `inherently`    `intrinsically`    `substantially`

---

`opt out`

选择不参与，退出:

- You can <u>opt out</u> of the new policy by manually deleting your Google web history.

**Collocations**: `opt out of`    `opt out option`    `opt out clause`    `choose to opt out`    `opt out form`

**Related**: `withdraw`    `pull out`    `back out`    `decline`    `abstain`    `exclude oneself`    `opt in`

---

`manually`    /ˈmænjuəli/

(adv.) 手动地，人工地:

- You need to <u>manually</u> delete the files instead of using the automatic cleaner.

**Collocations**: `manually enter`    `manually adjust`    `manually delete`    `manually control`    `performed manually`

**Related**: `by hand`    `automatically`    `manual`    `hands-on`    `physically`    `digitally`    `mechanically`

---

