当 “不论顺序” 的时候，不考虑顺序，排列组合的话，多种情况，考虑用 map 去重



当要判断长文本是否相同时，可以使用哈希表压缩数据，然后生成摘要编码，用来表示一个数据，快速判断是否相同。摘要之后，可以去除排列顺序的影响，但是要注意摘要的编码算法要确保不会出现二义，出现误判。例如：纯数字编码，不同的数位表示不同的含义，但是由于是纯数字，把不同位置的编码组合起来之后，整体无法拆分成单个，对应是出错，就是从局部到整体的映射是正确的，但是从整体到局部的映射会出问题。固定编码数据长度、防止越位、分隔符等措施，保证摘要编码的正确性。





